Bash(curl -s -X POST "http://localhost:3456/eval?target=FD09DDF8E83E659B7F70BA80CD9AA2F1" --data-binary @- <<'EOF'
      (() => {
        const el = Array.from(document.querySelectorAll('span,button,a,div,li')).find(el => (el.innerText ||
      el.textContent || '').trim() === '未读');
        if (!el) return {clicked:false, reason:'not found'};
        el.click();
        return {clicked:true, tag:el.tagName, cls:el.className, text:(el.innerText||el.textContent||'').trim()};
      })()
      EOF)
  ⎿  {
       "value": {
         "clicked": true,
         "tag": "SPAN",
         "cls": "",
         "text": "未读"
       }
     }