## 更新内容（8月10日）

### v1.3.21 更新内容

- **推荐牛人页兼容「推荐 / 精选 / 最新」三种导航**：之前页面顶部切到「最新」时，候选人卡片结构不同，工具找不到卡片会报「推荐牛人页面未加载」。现在三种导航都支持，提取、滚动加载、自动打招呼都能正常使用（包括「最新」下按 geekId 定位卡片）
- **滚动加载兜底**：页面由浏览器整体滚动时（「最新」导航），改用窗口滚动继续加载更多候选人，不再卡在列表中间

### v1.3.20 更新内容

- **AI 评级理由排版修正**：导出 Excel 时，评级理由现在会按评分模板**强制排版**——无论 AI 怎么输出，都会自动补上编号（1.首句定性 / 2.评估维度 / 3.任职资格 / 4.学历核查 / 5.综合结论）、板块标题和换行，不会再挤成一坨
- **教育经历解析修正**：修复学生组织名被误判为学历的问题（如「XX大学 XX学院研究生会」中的「研究生会」被当成研究生学历），现在只识别真正的学历教育记录
- **推荐牛人页加载失败提示更清楚**：页面迟迟没加载出来时，报错会附带页面真实状态（当前网址、页面标题、卡片列表类名、是否有登录提示），方便定位原因
- **过滤无关报错**：脚本往统计服务器上报状态失败（Stats report failed）不再混进错误提示，报错只显示真正的问题

### v1.3.19 更新内容

- **教育经历补全**：之前教育经历只显示最高学历，现在从简历文本解析出完整多段教育经历，按最高学历在首行、时间由近到远排序
- **教育经历解析修正**：修复校名粘连（课程名与校名连在一起被误判）、院系后缀（大学名后跟「经济管理学院」等院系名导致错名）、中学全名（「XX 附属中学」不再被拆成专业）、平台前缀标签（页面标签与校名粘连）、QS 排名误杀（带 QS 排名信息的学校不再被漏掉）、年份截断等解析问题
- **评分程序化计算**：匹配度评分不再直接信 AI 手写的数字（算术经常对不上公式），改为从评语解析各维度「独立得分 × 权重」程序化重算加权基础分，再减其他扣分——分数与评语内容严格一致
- **清理废弃文件**：移除已不使用的 scoretext.md 评分输出格式模板引用（评分提示词已包含完整格式），不再打包该文件

### v1.3.18 更新内容

- **打招呼评分与输出结果对齐**：打招呼等级过滤改为以评语中的「匹配度评分」为准，与界面上看到的分严格一致——之前 AI 返回的 score 字段和评语里写的分数偶有不一致，会导致打招呼范围对不上
- **学历因素只减不加**：学历只在「其他扣分合计」中扣分，任何情况下不得因学历或院校层级（985/211/双一流等）上调最终评分
- **评分标准统一为 20 分档**：81-100 / 61-80 / 41-60 / 21-40 / 0-20，各档描述更清晰
- **评分模板合并**：去掉重复的「评语内容规范」，统一为一份「最终输出模板」，匹配度评分整篇只出现一次
- **教育经历解析修复**：简历文本里超长的教育经历段落自动切段识别，粘连进校名的描述性内容（如「文化活动与会展策划」等）正确剥离，正常校名不受影响

## 下载
| 文件 | 说明 |
|------|------|
| Boss.AI.Setup.1.3.21.exe | 安装包 |
| win-unpacked.zip | 绿色版（解压即用） |
