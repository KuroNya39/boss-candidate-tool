## 更新内容（9月24日）

### v1.17.0 更新内容

**学历写得更全，导出的教育经历更准**

- AI 评语里的每一段学历现在分开写全：**学校 | 专业 | 学历层次 | 就读时间 | 全日制/非全日制**，不再几段挤成一行、也不再漏掉就读时间
- 导出的 Excel 教育经历据此修正，学校、专业、学历、时间各归各位。以前简历里写成「机械设计制造 大专」这类（专业紧跟学历词）的，专业会被学历词吞掉、整格空着，现在能正确读出来
- 学历词表补全了：**专升本、专科、高职、高专、中技、技校、职高、学士**这些写法以前认不出来，现在都能识别，教育经历的排序与判断跟着更准

**第一学历的判定收严了（分数会变）**

- 以前只有第一学历写着「大专 / 专科」或明确标注「非全日制」才扣 20 分；现在只要第一学历低于全日制本科，一律扣 20 分——**中专、技校、高职、职高、高中、成人本科、自考、函授等以前漏掉的，现在都会扣**
- 学历行按新格式写全后，判定只看「学历层次」与「全日制/非全日制」两段，不再被专业名、备注里的字干扰
- 因此**同一份简历重新评分，分数可能比上次低**。已跑过的批次不用重新提取，在「历史记录」里点「评分」就能按新口径重评

**岗位列表按拼音排序**

- 「目标岗位」列表里的中文岗位名以前按编码顺序排（跟读音毫无关系，看着就是乱序），现在按拼音排；字母、数字开头的岗位排在最前
- 岗位名里的数字按数值比：「P7」现在排在「P10」前面，不再按字符逐个比

**评语排版统一**

- 打分时写进数据的评语，断行规则和完成页、Excel 里看到的现在完全一致（以前是两套，同一段评语在软件里和 Excel 里的换行位置不一样）

**更稳更干净（内部整理）**

- 学历词表统一到一处，提取、评分、导出共用同一份，不会再出现「这里认得出、那里认不出」
- 提取、打招呼、导出这几个脚本补上了「只在自己被直接运行时才执行」的保护，被别的程序加载时不会再误触发（那些动作会真的去点浏览器、占用系统剪贴板）
- 去掉一个早已没人调用的旧解析函数；几处每次调用都重建的正则改成只建一次。功能与界面表现和之前一致

## 更新内容（9月23日）

### v1.16.0 更新内容

**输入框右键菜单**

- 设置、添加岗位等所有输入框和多行文本框里点右键，弹出的是软件自己的菜单：剪切 / 复制 / 粘贴 / 删除 / 全选，每项右侧标着对应快捷键
- 以前弹的是系统菜单，配色、圆角跟软件对不上；现在与下拉菜单同一套观感，方向键也能上下选
- 剪切 / 复制 / 删除在没有选中内容时置灰，只读的框里粘贴置灰

**点「跳过」或「取消」不再丢当前这位候选人**

- 取消或跳过时会先等当前这位候选人的简历识别收尾（最多 20 秒）再保存进度；以前最多等 3 秒就掐断，他可能只存下半份简历
- 等不到就把他整条剔除，下次提取重新抓 —— 不会再出现「进度里记着他、简历却是空的」这种条目（这种条目恢复后会拿空简历去评分，分数没有意义）

**许可协议与版权**

- 软件改用 **PolyForm Noncommercial 1.0.0** 许可：个人非商业使用免费
- 协议全文随安装包一起放在安装目录里；安装包和程序文件的属性（右键 → 属性 → 详细信息）里补上了版权信息

**界面细节**

- 「正在导出」这类按钮里的转圈图标与文字之间空出一大截的问题修好了
- 历史记录批量删除失败时，提示的单位从「个」改成「条」

## 更新内容（9月22日）

### v1.15.2 更新内容

**导出的 Excel 行高统一了**

- 以前每个候选人的行高是按**在线简历的字数**自动撑出来的：简历越长行越高，短的 1000 磅出头、长的能到 11000 多磅，同一张表里差十几倍，翻起来一会儿一小行、一会儿十几屏
- 而且这些高度**全都超过了表格单行能显示的上限**（409.5 磅），超出部分各家表格软件（WPS / Excel）处理方式还不一样，显示效果没法预期
- 现在**每个候选人固定占 409 磅**（表格能显示的最大高度），整张表行高一致，翻起来每行都一样高
- 说明：AI 评语基本能完整看到（最长的会差几行）；**在线简历只看得到开头**，要看全文点一下单元格，在上方编辑栏里能读到完整内容

## 更新内容（9月21日）

### v1.15.1 更新内容

**导出的 Excel 表格配色重做**

- 列底色从原来的几种扩到 **8 种柔和的浅色**，按内容分块各占一色：AI评分（淡黄）、AI评语（淡绿）、个人情况（淡粉）、教育经历（淡橙）、在职情况（淡青）、求职期望（淡紫），姓名与在线简历各一色
- 三组标题条改用 **蓝 / 绿 / 黄** 三种颜色配白字，「AI分析 / 基本信息 / 求职期望」的分组边界比原来清楚得多

**列顺序与列宽调整**

- **求职状态**列从教育经历右边挪到**工作年限右边**，「个人情况」四项（活跃度、年龄、工作年限、求职状态）现在连在一起
- **教育经历**四列顺序调整为 **学校 → 专业 → 学历 → 时间**，按「先看学校、再看专业」的习惯排
- **学校**与**专业**两列改为同宽，并比原来宽一点，长校名和长专业名（如「机械设计制造及其自动化」）都能放下

**完成页的姓名光标范围收窄**

- 候选人列表里，鼠标移到姓名上才显示「可选中」的 I 形光标；以前整行除了分数那几块都是 I 形，看着像整行都能选字
- 拖拽选中复制本身不受影响，照常可用

### v1.15.0 更新内容

**导出的 Excel 多了 7 列候选人信息**

- 新增 7 列：**活跃度**（刚刚活跃、3日内活跃…）、**求职状态**（离职-随时到岗、在职-考虑机会…）、**在职企业**、**在职岗位**、**在职时间**、**期望岗位**、**期望行业**
- 表头按「AI分析 / 基本信息 / 求职期望」三组分色排布，三组各一种颜色，一眼能分清边界
- 四列表头更名：AI评级 → **AI评分**、AI评级理由 → **AI评语**、当前公司 → **在职企业**、当前职位 → **在职岗位**
- 「教育经历」表头不再加粗；「姓名」与「在线简历」两列的表头合并成整块，与其它列对齐

**简历正文不再漏掉姓名与活跃度**

- 画布型简历靠模拟「选中复制」取正文，起点原先压得偏低，会把候选人**姓名和活跃度那一行**留在选区上方、复制不到
- 起点上移到画布最上沿后，姓名与活跃度一并复制进来，活跃度那一列也就有值了

**完成页的姓名可以拖拽选中复制**

- 完成页的候选人列表里，整行点击是展开/收起评语。现在**姓名上可以直接拖拽选中并复制**（双击选词也行）
- 拖选不会再顺带把评语展开；点分数、点箭头照常展开收起

**说明文档（README）措辞与排版统一**

- 「运行中的注意事项」前两条互换位置：先说「请勿最小化 Chrome」，再说「请勿把 BOSS直聘切到后台标签页」
- 「速度参考」从「运行中的注意事项」移到「日常使用」，放在「运行过程」之后

## 更新内容（9月18日）

### v1.14.1 更新内容

**搜索框提示文字改回原样**

- v1.14.0 里目标岗位搜索框的提示文字被改成了「搜索岗位或拼音首字母」，这一版改回原来的「搜索岗位」
- 拼音首字母搜索本身不受影响，照常可用：输入 `xsqd` 仍能搜到「显示驱动工程师」

### v1.14.0 更新内容

**目标岗位搜索支持拼音首字母**

- 以前「目标岗位」列表只能用岗位名里的字来搜，必须打出「显示驱动」才能筛出「显示驱动工程师」
- 现在打**拼音首字母**也行：`xsqd` → 显示驱动工程师、`sz` → 深圳、`zh` → 珠海、`gcs` → 工程师。大小写随便打，`XSQD` 一样能搜到
- 多音字**按词判读音**：同一个字在不同词里读音不同时，取的是它在这个词里的读音（比如「重庆」读 chóng 不读 zhòng），首字母不会取错
- 岗位名里的英文和数字照常能搜：`35k` 直接筛出薪资段含 35K 的岗位
- 拼音表覆盖常用字与次常用字，深圳的「圳」、东莞的「莞」这类不在最常用字表里的字也能取到首字母

## 更新内容（9月16日）

### v1.13.4 更新内容

**安装程序在高分屏上不再发糊**

- 安装包内嵌的清单里以前**一条 DPI 声明都没有**（装完的程序本身是有的，所以只有安装过程那一屏糊）。Windows 缩放不是 100% 时（常见的 125%、150% 都算），系统会把没有声明的安装窗口整个当图片放大，字和控件全糊成一片
- 现在给安装程序补上了 DPI 声明，界面按原生像素渲染，清晰；另外补了「逐显示器感知」，安装窗在不同缩放的显示器之间拖动时不会被拉伸错位
- **需要知道的取舍**：安装程序的界面本身不会跟着缩放，所以在高于 100% 缩放的机器上，安装窗口会比以前**小一圈**（字和按钮都变小）。清晰和小尺寸之间，本次优先保证清晰

### v1.13.3 更新内容

**提示气泡的尖角重做：不再比气泡先亮**

- 「开启模拟复制提取」右边 ℹ️ 悬停弹出的提示气泡，它下面的小三角以前是拿一个方块转 45° 做的，上半截塞在气泡底下（靠气泡把它盖住，才露出一个三角）
- 两块叠在一起时，淡入过程中重叠处的颜色会叠得更深——气泡还没亮到位、尖角那一截先深了，看着就是两个图层各亮各的
- 现在尖角改成图标库里的箭头图（Material Symbols 的 `arrow_drop_down`），**整体挪到气泡正下方、与气泡严丝合缝地上下相接，一点都不重叠**，显隐和上浮与气泡共用同一套规则。逐帧采样复核过：两边的透明度和位移每一帧数值完全一致，看着就是一枚连体的气泡
- 尖角指向、大小、位置都没变：仍正对 ℹ️ 的竖直中线（实测与图标圆心差 0.11px），底尖仍在图标上沿上方 2px，两条腰仍是 45° 左右对称

**README 配图更新**

- 图2（主界面）、图4（设置弹窗）、图5（编辑岗位弹窗）换成当前版本

## 更新内容（9月15日）

### v1.13.2 更新内容

**历史记录弹窗打开不再闪一下**

- 以前点开历史记录，弹窗会先以「一个空盒子」出现，过一瞬间再上下弹开撑到真实大小。原因是它先开窗、再等磁盘读数据（实测约 140ms），而弹窗高度是由条目撑出来的：空列表时只有 133px 高，数据回来了才变 400px 以上，盒子又居中对齐，于是上下同时往外弹
- 现在改成**数据到手才开窗**，弹窗一出现就是最终大小，中间没有任何变化；这点等待也感觉不到——点菜单项时菜单正在播 150ms 的收起动画，数据回来时它刚好退场
- 顺带修掉同一个毛病的另一半：删除一条记录后刷新列表时，以前会先把列表清空（弹窗当场塌一下）再填回来，现在清空与填充在同一帧完成
- 关窗后焦点回到左上角菜单按钮的行为不变（键盘操作、Esc 关闭都复核过）

**标题行高收紧：字号越大，行高倍数越小**

- 按排版惯例（字体越大行高倍数越小）把标题的行高从正文的 ×1.5 收到 ×1.2，涉及：页头标题、模块标题（执行进度那张卡）、4 个弹窗标题（设置 / 历史记录 / 添加新岗位 / 目标岗位）、完成页与出错页的标题
- 实际观感：顶栏高度不变（标题在栏里居中，多出的空间被吸收），弹窗和完成页各矮 5px，进度卡矮 2px；没有任何文字被挤到或错位
- 正文（14px）、评分理由、按钮和列表行上的单行文字都不受影响——按钮那类单行文字的行高是用来撑控件高度的，动了等于改全站按钮和输入框的高度，故单独豁免
- 设计系统 §3 把行高这条规矩写清楚了：**行高 = 字号 × 倍数**，标题 ×1.2 / 正文 ×1.5 / 小字长段落 ×1.8，单行控件 ×1.4（豁免，附理由）；四档之外不许再出现行高字面量
- 顺带补一处隐患：分段钮、岗位选择框、下拉、顶栏菜单、结果列表行标题这些按钮以前没写行高，一直在吃浏览器默认值（约 1.3），高度不受设计系统管。现在把这一档行高兜在「按钮」这个总规则上（而不是逐个控件去补），以后新增的按钮也不会再漏。补上后这几处各高 2px；岗位选择框和结果行标题的高度是固定的 40px，外观不变

## 更新内容（9月14日）

### v1.13.1 更新内容

**邮件主题按来源页统一**

- 三个页面发出去的邮件，主题现在长一个样：沟通页「沟通页评分结果」、推荐牛人页「推荐牛人页评分结果」、搜索页「搜索页评分结果」
- 沟通页以前是「候选人评分结果」，推荐牛人页少一个「页」字，收件人看不出这批人是从哪个页面抓的
- 邮件正文一个字没变，还是「候选人评分结果已生成，Excel 文件见附件。」

**修复：历史记录里的蓝色「提取中」标错状态**

- 那个蓝标以前基本用不上，还容易误标：提取真正在跑的时候，显示的反而是黄色「未完成」（因为提取过程中进度文件一直在）；只有提取刚结束、AI 评分还没出结果那一小段它才会变蓝
- 更麻烦的是，在这一小段里要是点了停止或评分报错，这一批就永远挂着蓝色「提取中」，明明什么都没在跑
- 现在改成按「这一批此刻是否真在跑」判断：跑着就是蓝色**进行中**（提取、评分、导出任一阶段都算），没在跑又没出结果就标黄色「未完成」——和归档批次一个标法，可以点「评分」把结果补齐

### v1.13.0 更新内容

**界面整体过了一遍：按钮反馈统一、图标换新、卡片分层更干净**

- 普通按钮鼠标悬停不再上浮，只改底色；实心蓝按钮、红按钮（删除等）悬停由「变深」改为「变浅」，看着轻快些
- 主按钮「开始提取分析」的悬停加深 + 上浮 + 阴影保持原样，不受影响
- 浅蓝底的条目（顶栏菜单项、下拉选项、岗位列表行、候选人结果行）按下时不再明显变深，与悬停色一致
- 图标整套换成新版 Material Symbols，字形更统一；输出目录的文件夹图标悬停不再变蓝（改为变深灰），提示图标（ℹ️）新增悬停 / 按下加深，以前悬停毫无反应
- 页面底色由浅灰改纯白，卡片靠阴影分层；阴影四边都有且浓淡一致，卡片上 / 左 / 右不再「缺角」
- 几处图标尺寸微调（提示、密码眼睛、文件夹、邮件失败箭头、顶栏菜单），视觉大小与旁边的字更齐

**历史记录弹窗重新排过**

- 每条记录由 3 行压到 2 行：时间在左上、人数在右上，状态胶囊与按钮同行
- 按钮顺序改为「继续提取 / 删除 / 打开目录 / 评分」；「继续提取」降为空心蓝，「评分」升为实心蓝（最右，唯一的主按钮）
- 「删除」「打开目录」改为无框灰色图标按钮（删除以前是红框文字按钮），操作风险交给点下去之后的确认弹窗
- 当前批次的「删除」以前干脆不显示，现在显示但灰着（鼠标移上去能看到原因），行末不再跳一下
- 时间文字由近黑降为灰色（以前太抢眼），人数右缘与按钮组对齐

**交互相关的几处修复**

- 滚动条不再「一会儿有一会儿没有」：岗位列表、候选人列表、设置表单的滚动条现在鼠标移进去就出现、移开就隐藏（与岗位描述输入框一致）
- 鼠标停在设置弹窗的输入框标签上时，输入框不再隔着老远就亮起来（标签以前会把悬停转给输入框）。代价：点标签不再跳到对应输入框，请直接点输入框
- 来源分段条（沟通 / 推荐 / 搜索）按住或拖动时高亮不再消失；按住未选中的那格现在显示淡蓝底，按着就能预览会选中哪档
- 首页未选岗位时点「开始提取分析」，提示弹窗点「去选择岗位」不再闪一下
- 用 Tab 键走到「邮件发送失败」那行和复选框时，不再冒出系统自带的橙黄色方框
- 弹窗、页面标题这类只给键盘 / 读屏用的落点，随便按个键（含 Shift）不再冒出一圈橙黄框
- 候选人结果行、设置弹窗的输入框，聚焦时的蓝框以前可能被列表边缘切掉，现在完整可见

**其他**

- 岗位描述的输入框改为两端对齐，左右边缘整齐
- 完成页的统计数字、打招呼模块标题由近黑改为灰色（以前偏重）
- 步骤编号 1 / 2 / 3 / 4 与结果人数改用等宽数字，编号在圆圈里不再看着偏
- 打招呼跑完后界面立刻解除「已有任务运行中」，不再等子进程收尾的那几秒（以前这几秒里点什么都回「已有任务运行中」）
- 打招呼脚本内部的 30 秒超时定时器用后即清，进程不再被吊住；并修掉一个「上一轮刚结束、新一轮刚开始时，界面可能误判没有任务在跑」的隐患

**文档**

- README 里历史记录的操作顺序跟随新按钮顺序更新

## 更新内容（9月11日）

### v1.12.7 更新内容

**修复：提取时「继续」按钮鼠标移上去文字变黑**

- 提取过程中点「暂停」，按钮会变成琥珀色的「继续」；此前鼠标悬停时文字会变成黑色，看着像坏了，现在悬停时底色加深、文字仍是琥珀色
- 只影响这一个按钮的悬停显示，功能和使用方式不变

### v1.12.6 更新内容

**提示文案的标点统一成中文写法**

- 冒号、括号、省略号、引号不再中英混着用：冒号一律「：」、括号一律「（）」、省略号一律「…」、引号一律「「」」（网址、端口这类不能动的照旧）
- 括号前多余的空格去掉了：「已连接 Chrome （端口 3456）」现在是「已连接 Chrome（端口 3456）」
- 修掉几处不一致：打招呼日志里有一处括号半边是英文的；输出目录名的报错里多一个空格
- 「保留页面标签页」「已附着到用户打开的沟通页标签页」这类重复说法精简为「保留标签页」「已附着到用户打开的沟通页」

**几处说法更明确**

- 「滚到底部后又加载了更多」补上主语，改为「滚到底部后又加载了更多候选人」
- 底部等待超时那句改为「超过 12 秒仍无新候选人，按已到底部处理」
- 复制途中标签页被切走的提示改为「复制进行中标签页被切换，已自动切回 Boss 页继续…」，重试提示改为「第 1 次未成功，切回 Boss 标签页重试…」
- 「{人数} 人当前无打招呼按钮、未打招呼」的顿号改为逗号

### v1.12.5 更新内容

**提示文案整体过了一遍：说法统一、少让人猜**

- 找不到 Chrome 时不再让你去翻 README，直接说「请正常打开 Chrome，开启远程调试后使用」
- 设置没填全的提示从「请把设置填写完整（API 地址、Key、模型名称）」精简为「请填写完整的设置信息」
- 邮箱密码的提示改写得更明确：要填的是「三方客户端安全密码」，不是邮箱登录密码
- 没找到可评分数据的报错说清两条路：「点击开始提取分析进行提取」或「使用上次已提取的数据进行评分」
- 用词统一：「tab」一律改叫「标签页」，句子末尾补上句号；打招呼结果里「几人没成功」改为「几人未成功」
- README 第 4 步的邮箱说明同步更新

## 更新内容（9月10日）

### v1.12.4 更新内容

**修复：历史记录里的来源、岗位被下一批顶掉**

- 连着跑两批不同来源时（比如先跑沟通页、再跑推荐牛人页），第一批的历史记录会显示成第二批的来源，岗位也跟着变成第二批的——表现就是「明明是沟通页跑的，却标成推荐牛人页」
- 原因：收尾归档时多归档了一次，把第二批的「运行记录」卷进了第一批的文件夹；现在每批只归档一次，历史记录里的来源、岗位、人数都和这批数据对得上，「继续提取」也不会还原成别的岗位
- 电脑里已经存在的历史批次保持原样，不影响新跑的批次

**其他**

- README 去掉完成页截图

### v1.12.3 更新内容

**修复：沟通页的「模拟复制提取」**

- 沟通页上有时还留着一份看不见的旧简历弹窗，提取会被它误导，挑错要复制的那份简历：表现为页面不往下滚动、拖动起点也偏，复制到的内容不全
- 现在改成按「屏幕上真正显示的那份」来提取，推荐牛人页 / 搜索页行为不变

**修复：提取结束后把你剪贴板里的东西还回来**

- 之前复制的内容如果比较长、或者本来就是空的，还原会静默失败（你的剪贴板被留下简历全文）；现在这两种情况都能正确还原

**其他**

- 读取剪贴板时 PowerShell 的报错信息以前会混进日志面板、显示成一段乱码，现在不再出现

### v1.12.2 更新内容

- 「⋮」图标和它右边的「设置」二字现在垂直对齐了（齿轮这种上下对称的图标，差半像素眼睛就能看出来）
- 设置里的「邮件通知（可选）」改名为「邮箱地址（可选）」；没填地址时，下方的「邮箱密码」框和眼睛按钮是灰的、点不动，先填地址才能填密码（以前填过的密码不会丢）
- 密码框（API Key、邮箱密码）每次打开设置弹窗都回到隐藏状态，不会把上次点开看过的明文留在屏幕上
- 修掉一个容易丢数据的小毛病：在设置弹窗里的长输入框按住鼠标拖动看后面的内容，松手时指针已经拖到弹窗外，以前会被当成「点了空白」把弹窗直接关掉，没保存的内容就没了；现在只有按下和松开都落在遮罩上才算数

### v1.12.1 更新内容

- 左上角「⋮」的悬停/选中底色框现在正对图标居中（原来整块偏右 7px，看着像没套住图标）
- 左上角「⋮」的图标颜色调浅一档，和界面上其他图标一致

### v1.12.0 更新内容

**首页变清爽了：设置和历史记录收进左上角菜单**

- 原来占着首页一大块的「设置」折叠卡搬走了，首页只剩「连接状态」和「运行配置」两块，一屏看全、不再拥挤
- 左上角新增「⋮」菜单，点开就是「设置」和「历史记录」两项；点菜单外面、按 Esc、拉窗口都能收起
- 「设置」和「历史记录」现在都是居中弹窗：窗口拉矮时只有中间内容滚动，标题和底部按钮始终在；Esc、点遮罩、「关闭」都能关掉
- 标题现在稳定居中，版本号移到最右边

**「提取来源」可以按住拖了**

- 三个来源按钮除了点，还能按住鼠标左右拖动切换：选中高亮跟着手滑，滑到哪就选中哪，松手后平滑停住
- 单纯点击仍是原来的手感，不受影响

**「开启模拟复制提取」不再顶掉你复制的东西**

- 以前提取画布型简历时，系统剪贴板里的内容会被简历全文替换掉、而且不会还回来；现在只在提取的那几秒占用，结束（含失败、超时）一律原样还给你
- 顺带收短了几处纯等待的时间，每个候选人能省下零点几秒

**其他细节更顺手**

- 目标岗位列表里的「删除」改成和「编辑」一样的灰色描边：两个按钮并排时不再显得删除更重一级（红色留给「清空历史记录」这类真正不可逆的操作）
- 「人」字与上方的来源条、下方的按钮右边缘对齐
- 来源按钮的槽底加深一档，白卡片里分段条的轮廓更清楚；悬停底色微调（悬停 5% / 选中 10% / 悬停选中 15%），三档间距更均匀
- 鼠标移到「ⓘ」上方时，提示气泡下面多一个小尖角指向图标，不再是没来由的一个方块
- 用键盘的用户：从菜单打开历史记录再关掉，光标会正确回到「⋮」，不会掉回页面开头
- 界面文档（README、设计系统）同步更新

## 更新内容（9月9日）

### v1.11.1 更新内容

**修复：切换来源页不再弄丢「完成后自动打招呼」的勾选**

- 之前勾了「完成后自动打招呼」后切去搜索页 / 沟通页再切回推荐牛人页，勾选会被悄悄清掉、但下面的等级下拉还留着，看着像坏了
- 现在勾选会一直保留：切走再切回，勾选和等级下拉都照旧；真不想要时自己点掉勾选，下拉也会跟着收起

### v1.11.0 更新内容

**打招呼没成功的人，可以单独「重试」了**

- 一批打招呼跑完后，若有人「点了没成功」（多数是打招呼太频繁、被平台暂时限制），结果会以警示色提示，并说清成功几人、几人没成功
- 这时主按钮会变成「重试」：点它只给这批没成功的人再打一轮，已经成功打招呼、已经沟通过的人不会被重复打扰
- 再打仍有失败，按钮会继续保持「重试」，过会儿再点即可；全部成功后按钮自动回到「开始打招呼」
- 「页面里找不到的人」不算失败、不会重试；换档位或回到首页会作废旧的重试，重新整批开始

**界面更统一顺手**

- 「提取来源」三个按钮切换时，选中高亮会平滑滑过去，不再生硬跳变
- 下拉框（如打招呼等级）展开、收起现在都有对称的过渡，收起不会「啪」一下消失
- 下拉选项与各处的选中 / 悬停底色统一成同一套浅蓝，和来源按钮一致，观感更整齐

**更稳更干净（内部整理）**

- 打招呼「重试」的状态统一归到一处管理、去掉重复逻辑；功能与界面表现和之前一致

### v1.10.5 更新内容

**按钮「加载中」的样子更明确、也不会卡住**

- 运行中点的按钮（「跳过提取」、顶栏「重试」等）在等待时，按钮文字保持原样（不再变成「正在跳过提取…」「重试中…」），只在前面加一个转圈小动画表示在工作
- 等待期间按钮会真正禁用：怎么点都没反应，不会重复触发跳过或重试
- 顺手修好一个隐患：以前万一这步没正常收尾，按钮可能一直灰着转圈；现在无论成功失败都会自动恢复

**跳过提取的确认弹窗文案更顺口**

- 标题改为「跳过提取？」；说明改为「确定跳过剩余候选人，用已提取的数据开始 AI 评分吗？」

**键盘细节更干净**

- 键盘 Tab 到真正的按钮 / 输入框时才有蓝色焦点框；切换页面后落点的标题等非按钮文字不再误显示焦点框

**更稳更干净（内部整理）**

- 按钮加载状态统一交给一处管理、去掉重复与冗余代码、界面文档同步；功能与界面表现和之前一致

### v1.10.4 更新内容

- **修复：扫描候选人列表时点「暂停」能停了**：以前步骤1 显示「正在扫描候选人列表…」往下滚着找人时，点「暂停」没反应，要等列表扫完才开始逐人提取才停得下来；现在扫描列表阶段点「暂停」也会马上停下，点「继续」接着往下扫
- **「提取数量」手输也有上限了**：以前点箭头最多加到 999，但直接往框里敲 999 以上照样能填、软件也会照那个数去跑；现在无论点、键盘还是手输，最多都是 999——手输超过 999 会自动收成 999
- **打招呼提示语改成「正在打招呼…」**：点「批量打招呼」或「完成后自动打招呼」后，提示从「正在打招呼」改为带省略号的「正在打招呼…」，后面跟着的人数进度（如 `1/40`）不变

### v1.10.3 更新内容

**界面更清爽、层次更分明**

- 字体与粗细全面梳理：正文、标签、步骤标题、候选人姓名不再刻意加粗，标题、选中项和分数数字才加粗。此前不少地方用的「中粗」在系统字体下显示不出区别，现在全局只保留「常规 / 加粗」两档，界面更干净利落
- 「提取来源」三个按钮的当前选中项从实心深蓝改为浅蓝色块：一眼看清选了哪个来源，也不再和下方深蓝的「开始提取分析」主按钮混淆
- 「开始提取分析」按钮去掉前面的播放图标，纯文字更简洁
- 顶部软件名改为居中显示，版本号贴到右侧，标题不再被版本号挤偏

**窗口比例与留白更舒服**

- 默认窗口形状调成竖长一点的比例（宽:高约 0.91），整体更协调；内容仍随窗口等比缩放、不截断
- 卡片阴影再调浅一档，观感更亮更干净；启动瞬间的窗口底色也与界面底色统一，不再有一闪的色差
- 「运行配置」卡片去掉了自己的标题、上下留白加大，一进来就是可用操作，更聚焦

**小细节更顺手**

- 「提取数量」框里数字改为偏左显示、不再顶到右侧箭头；「模拟复制提取」后的提示圈与文字间距收紧
- 下拉框（如打招呼等级）一打开箭头就翻成朝上（提示「已展开」），不再跟菜单上下方向变化

**候选人评分理由更整齐**

- 候选人详情里多行的评分理由改为两端对齐，左右边缘整齐，读起来更舒服

## 更新内容（9月8日）

### v1.10.2 更新内容

**界面更清爽一点**
- 页面底色（卡片之间露出的浅灰）再调浅一档，整体更亮、更干净

**模型名称填写示例改清楚**
- 模型名称输入框的提示语改为 `gpt-5.6-…`，一眼知道大概填什么样的模型名

### v1.10.1 更新内容

**弹窗来回切换不再闪**

- 「目标岗位」列表和「添加 / 编辑岗位」之间来回切换时，以前中间会空一下、闪一下；现在改成两张卡片交叠、同时淡入淡出，切换顺畅不闪

**应用图标换成品牌青绿色**

- 窗口、任务栏、安装包的图标保留原来图形样式，整体颜色换成 BOSS 直聘品牌色 #00c1c1，更贴合品牌

**输入框提示语更清楚**

- 岗位名称输入框不再显示灰色示例文字
- 岗位描述框的提示语改为「粘贴转换后的岗位描述，请勿直接粘贴原始岗位 JD」

**更轻更快（内部整理）**

- 主进程与界面代码按功能拆分整理，去掉不再使用的依赖与脚本，安装包更小、打包更快；功能与界面表现和之前一致

## 更新内容（9月7日）

### v1.10.0 更新内容

**窗口可以自由放大缩小了**

- 以前窗口是固定大小、拉不了；现在可以**拖窗口边角整体等比放大缩小**（像缩放一张图片，拉不成扁 / 瘦形状），也可以最大化——小屏、大屏都用得更舒服，启动时会按当前屏幕自动取一个合适的大小

**界面整体重新整理**

- 卡片去掉了硬边框，改成白底加很淡的柔和投影，配合浅灰页面底色更清爽
- 页面四周和卡片之间的留白加大，内容不再挤着窗口边缘
- 主操作按钮的图标统一做实心（「开始提取分析」的播放键、「跳过提取」的跳过键），一眼能看出是主动作
- 输入框高度、按钮样式、提示图标等小细节统一成同一套，观感更整

**历史记录「评分」更通用了**

- 以前只有提取完整跑完的批次才能直接用已有数据评分；现在只要这个批次**提取出过简历**——哪怕只提了一半就取消、哪怕早就评过——都能对它「评分」或重新评分，数据被自动归档也能找回来，**不用重新提取**
- 首页原来的「直接用上次数据评分」按钮取消，统一挪到「设置 → 历史记录」里点该批次的「评分」

**报错更省心、结果更好回看**

- 提取时若 Chrome 开着但对应的页面（推荐牛人页 / 搜索页 / 沟通页）没打开，软件会**自动帮你打开那一页**，再弹一句「该怎么做」，不再是一长串看不懂的报错
- 截图 OCR 提取后，原始截图会保留在输出目录里，方便回看这批提取的内容对不对

**「停止打招呼」能立刻停**

- 批量打招呼时点「停止」，现在会马上收手：不再给后面的候选人继续发消息，也不用干等某个候选人的超时——以前要等几秒甚至 30 秒才能真正停下来，那段时间浏览器里可能已经多发出几条招呼

## 更新内容（9月4日）

### v1.9.11 更新内容

- **历史记录的状态胶囊更好认了**：同叫「已完成」，以前当前批是绿色、历史批却是灰色，容易看岔。现在统一——**已完成**=绿色、**未完成**=黄色（有提取没跑完的批次）、**提取中**=蓝色
- **状态排到最前面**：每条记录先显示状态（已完成 / 未完成 / 提取中），再显示来源（推荐牛人页 / 搜索页 / 沟通页），一眼先看到这批发没发完
- **「提取未完成」改叫「未完成」**：不再写具体是哪一步没完成——有些批其实已评完分，写「提取未完成」反而误导

### v1.9.10 更新内容

- **修复：列表还没加载完就提前停止提取**：列表滚到底部时，BOSS直聘有时会先显示「正在加载中……」、等一小会儿再陆续放出后面的候选人；以前软件一看到这行字就当「没有更多人了」直接停（实测 30 人只提取到 15）。现在推荐牛人页、搜索页、沟通页遇到这种情况都会先等它加载完、确认真的没有新人了才结束，不再漏人
- **推荐牛人页、搜索页滚动提速到和沟通页一样快**：往下找人时每步的等待从 0.8～1.5 秒（推荐页）/ 1.5～3 秒（搜索页）统一降到 0.3～0.6 秒，长列表不再慢慢爬（仍保留随机间隔，防止被 BOSS直聘识别成机器操作）
- **提取整体更跟手**：候选人之间的等待缩短到 0.3～0.8 秒、截图时每页间隔缩短到 0.1～0.3 秒，去掉了以前每提满 50 人就额外停顿 20 秒的防限制等待，大批量提取明显更快

### v1.9.9 更新内容

- **打招呼下拉框选项之间加了一条小空隙**：勾选中的选项和鼠标悬停的选项底色相临时，以前两块淡蓝色会连成一片分不清；现在选项之间隔出一点空隙，各自独立、一眼能分清当前指向哪个、勾选的是哪个（批量打招呼、完成后自动打招呼的下拉都适用）

### v1.9.8 更新内容

- **去掉标题下方的一条横线**：软件名「BOSS直聘候选人AI评分助手 v1.9.8」下面那条分隔线删掉了，顶部更干净
- **主界面间距调整**：标题与下方状态、状态与「设置」卡片之间不再离得那么开；「运行配置」卡片下方留出更多空间，底边不再贴住窗口底部；运行配置卡内部各分组的间距保持适度，整页仍然一屏装下

### v1.9.7 更新内容

- **打招呼下拉箭头方向修正**：打招呼 / 自动打招呼下拉框的箭头，菜单在下方弹出时朝下（∨）、空间不够、菜单转到上方弹出时自动变成朝上（∧）——箭头始终指向弹出的菜单，不再指反
- **「✓ 已保存」计时修复**：连续点好几次「保存设置」时，以前只按第一次点击计时、很快就会变回「✓ 已设置」；现在从最后一次点击重新计时，停留时间够长，不会让你误以为没保存上
- **浅底提示框统一去掉描边**：来源切换的黄色提示、邮件发送失败 / 报错的红字提示、打招呼结果提示等这类浅底卡片，边框都去掉了（浅底色本身就是区分），界面更干净
- **目标岗位「编辑 / 删除」按钮描边变淡**：不再是一圈很重的深色框，换成很淡的描边，点缀感更轻
- **主界面「请选择岗位」选中后改回浅灰框**：选中岗位后不再是一圈蓝边 + 淡蓝底，改回和输入框一致的浅灰框（鼠标悬停时才临时变蓝提示可点），整块区域更统一
- **顶部飘的「设置已保存」等通知去掉描边**：绿色 / 红色 / 蓝色的浮层通知卡片边框都去掉，只靠底色和文字区分
- **历史记录去掉「含 Excel」小标签**：每条记录的时间后面那个灰色小胶囊删掉了，更清爽
- **打招呼下拉选项底色统一**：鼠标悬停选项的底色和选中选项的底色现在一致，不再出现一深一浅的跳变（选中项靠蓝色文字区分）
- **清理一段没人用到的旧样式代码**：删除一个已被自定义下拉取代、无任何界面在用的旧下拉样式，界面无变化、代码更干净

### v1.9.6 更新内容

- **历史记录抽屉右上角的 × 去掉了**：那个 × 一直显示不对，现在直接移除。想关抽屉时，点抽屉外的空白处，或按键盘 Esc 键即可
- **滚动条不再碍眼**：主界面和历史记录抽屉平时不显示滚动条（滚轮 / 键盘照常能滚）；候选人列表、岗位描述编辑框的滚动条也只在鼠标移进框里时才淡出一小条，平时更干净
- **输出目录框改成了「只读值」的样子**：底色加深、去掉边框、文字变小变浅，一眼能看出这是给你看路径的、不能直接改；点右侧文件夹图标照常可以换目录。文件夹图标悬停不再出现一圈浅蓝底，只把图标变蓝，更轻盈
- **框线统一清理**：去掉几处多余的描边，保留「历史记录」「清空历史记录」等有意义的框线，界面更清爽
- **箭头图标换成同一套**：设置面板折叠箭头、下拉框箭头、候选人行的展开箭头，以前是系统自带的字符符号（▸▾），粗细和别的图标不统一；现在统一换成跟其余图标同一套的箭头图形，展开收起照常，旋转更顺滑
- **弹窗 / 抽屉进出动画同拍**：背景变暗和内容滑入 / 淡入的时长现在对齐，不再一前一后错位
- **标题、设置徽标等小细节微调**：页头标题略放大、设置状态徽标改成圆角胶囊样式，观感更整

## 更新内容（9月3日）

### v1.9.5 更新内容

- **历史记录抽屉的文字改干净了**：历史为空时只显示「暂无历史记录。」，不再附一句「跑完一批后按时间列出各批次」；抽屉底部那句「删除记录不会影响已导出的 Excel 文件」也删掉了——因为删除一条记录会连该批的 Excel 一起删，留着容易误导
- **「清空全部历史」改名「清空历史记录」**：按钮和确认弹窗标题都统一为「清空历史记录」；清空、删除单条两个确认弹窗里的「（不含已导出的 Excel 文件）」一并删除，说明与实际行为一致

### v1.9.4 更新内容

- **沟通页扫描再提速一点**：滚动到底部的复查等待上限从 6 秒进一步降到 5 秒，复查时每轮检查的间隔从 0.6 秒降到 0.5 秒，会话列表长的时候更跟手（仍保留随机间隔，防止被 BOSS直聘识别成机器操作）

### v1.9.3 更新内容

- **沟通页不再自动选择「未读」筛选**：以前跑「沟通页」时，软件会自动把会话列表切到「未读」，只处理没读过的会话；现在不再自动切换——你页面上当前显示哪些会话，就提取哪些。如果只想处理没读过的，先自己在 BOSS直聘沟通页点一下「未读」再开始即可
- **沟通页扫描滚动更快了**：滚动找人的每步等待从 0.8～1.2 秒降到 0.3～0.6 秒，「滚到底复查有没有漏人」的等待上限也从 10 秒降到 6 秒，会话列表长的时候明显更跟手（仍保留随机间隔，防止被 BOSS直聘识别成机器操作）
- **历史记录里的删除提示用词统一了**：删除某一条的确认弹窗、清空全部的提示，之前写的是「批次」，和界面标题「历史记录」对不上；现在统一叫「记录」（如「删除该记录？」「已删除 X 条历史记录」）
- **修复：历史记录不再可能出现「未知来源」**：批次的真实来源现在会随数据一起保存（扫描缓存、进度、结果文件都记录），即使中途只评分不提取、或个别文件缺失，历史记录、继续提取、重新评分也能正确认出该批来自「推荐牛人页 / 搜索页 / 沟通页」，评分时不再误标成沟通页
- **搜索页跑完后不再弹「搜索页打招呼说明」**：搜索页本来就不提供自动打招呼，那条「需要畅聊卡（付费）」的说明框已删除，完成页更干净（自动打招呼仍只有「推荐牛人页」支持）

### v1.9.2 更新内容

- **修复：历史记录里批次的「来源」偶尔被识别错**：推荐牛人页跑出来的批次，如果中途点过「直接用上次数据评分 / 跳过提取去评分」，历史记录里可能会被标成「沟通页」。原因：这种「只评分、不提取」的操作会顺手把批次的来源/岗位/数量记录覆盖成界面当时的状态（比如沟通页、岗位也被清空）——不只来源标签错，「继续提取」也会跟着拿错配置去沟通页跑。现在评分操作不会再改写批次记录，而是沿用这批数据真实的来源和岗位来评分；已误标的历史批次也会保留正确的来源标签

### v1.9.1 更新内容

- **页面没打开的报错改成一句「该怎么做」**：提取时报「某类页面没打开」时，以前会贴一长串「退出码 + 页面名 + 网址」，看着吓人又不告诉你要干嘛；现在只显示一句动作指引，如「请先在 Chrome 中打开 BOSS直聘「推荐牛人」页并设置好筛选条件，再点击「重试」」（搜索页 / 沟通页各有对应提示）；顶部连接状态的红字也从一长段缩短为「Chrome 未开启远程调试」
- **「执行出错」页新增「返回」按钮**：出错时除了点「重试」再试一次，也可以点「返回」回到主界面，调整提取来源 / 目标岗位 / 设置后再重新开始
- **顶部「重新连接 Chrome」按钮改名「重试」**：更短更直接，点击瞬间按钮上会显示「重试中…」
- **开始前不再弹「Chrome 已启动」提示**：以前软件自动帮你拉起 Chrome 后，还会再弹一个「Chrome 已启动」的提示，多余又打断操作；现在去掉了——Chrome 真没连上时统一提醒「请检查连接状态、允许 Chrome 远程调试」，并且先检查 Chrome 连接、连接正常后再检查有没有选目标岗位，顺序更合理
- **「请选择目标岗位」弹窗精简**：标题改为「请选择目标岗位」，正文一句话：「选择已有岗位，或点击「+ 添加新岗位」新建岗位。」
- **README 精简、推荐模型更新**：步骤说明与「运行注意事项」去重、措辞更口语化；「模型名称」填写示例更新为 `deepseek-v4-flash_DeepSeek`

### v1.9.0 更新内容

- **去掉「边用边跑」模式，点「开始提取分析」不再弹重启确认**：实测这个模式对「一边用电脑一边跑」并没有帮助（同一个窗口里切后台标签页照样失效），以前它还会时不时弹窗问「要不要重启 Chrome」，容易打断操作。现在开始前不再有任何重启弹窗——Chrome 在跑就直接用，没在跑会自动帮你打开 Chrome 并提示再点一次开始。想一边用电脑一边跑时，把 BOSS直聘单独放一个 Chrome 窗口、别点最小化即可（见 README「运行中的注意事项」）
- **「API 配置」面板改名「设置」**：软件左上角的折叠面板现在叫「设置」，保存按钮与成功提示同步改为「保存设置 / 设置已保存」，README 也已对应更新
- **修复：截图 OCR 偶尔结束后要干等很久才开始下一个人**：以前遇到多页长简历，上一个人的文字识别还没算完，会让下一个人截图完在原地干等几十秒（日志里「第 X 页」后长时间没有输出）；现在最多等 3 秒就开始处理下一个人，节奏更跟手
- **README 说明同步更新**：按新版行为重写运行说明与常见问题（开两个 Chrome 窗口的实测经验、别点最小化的范围、FAQ 措辞等），并修正开发者文档里岗位描述的存储路径写法

## 更新内容（9月2日）

### v1.8.5 更新内容

- **产品与平台名称统一为官方写法「BOSS直聘」**：软件标题、安装包、快捷方式、README、GitHub 简介以及界面提示里的「Boss直聘」全部改为官方大写的 **BOSS直聘**（产品名同步改为「BOSS直聘候选人AI评分助手」）。仅名称写法调整，功能与使用方式不变，已保存的岗位与配置不受影响
- **README 精简与措辞优化**：历史记录、分数档位、常见问题等章节文字精简；「运行中的注意事项」第 2 条「别点最小化」的说明改短、更直白

### v1.8.4 更新内容

- **修复：评分没开始时 ② 不再提前点亮**：批量提取中还没提满 15 人、评分没启动时，步骤2 的 ② 以前会跟着 ① 一起变蓝转圈，其实该和 ③ 一样灰着等；现在只有满 15 人、评分真正开始后 ② 才变蓝
- **修复：已经开始评分时提示语不再误报**：提满 15 人后评分其实已开始，但第一批结果还没返回时，以前还显示「等待更多候选人（满 15 人开始）」，看着像没在评；现在显示「已满 15 人，正在评分…」，有结果返回后再显示「已评 x / y 人」
- **历史记录「继续提取」只给最近 3 批**：更早的批次不再显示「继续提取」，只留「评分 / 打开目录 / 删除」——时间太久的提取进度对应的聊天/页面早就变了，接着续也续不上，还会让历史列表挤满按钮
- **发邮件时服务器中途断连会自动重试**：发送评分邮件偶尔会被邮件服务器中途掐断（报 Unexpected socket close），以前会直接失败要手动重发；现在会自动重试最多 3 次，只有密码/授权码这类真错误才直接提示

### v1.8.3 更新内容

- **切走标签页后，复制会自动把 Boss 页拉回来接着干**：复制简历时如果不小心切到别的网页，以前只有第一次会切回 Boss 页，之后再切走就不会了，结果要干等很久、最后走又慢又不清晰的截图；现在每次复制开始前都会先确认 Boss 页在最前面，就算复制进行中被切走导致卡住，也会约 6～8 秒内自动把 Boss 页切回来继续复制（最长约 20 秒兜底），尽量走又快又准的复制通道，不再干等 60 秒
- **新开一轮不会再拿上一批的简历提前评分**：刚点「开始提取分析」，AI 评分却已经用上一批的历史简历在打了——原因是上一批的数据没被顺利收走（比如上一轮的 Excel 还开着，导致整个文件夹归档失败）。现在新开一轮时会先把上一批的旧文件逐个归档走，再清掉残留的提取进度，保证这一轮从第一个人起评的都是全新的简历
- **暂停时评分会跟上提取的人数**：点「暂停」歇一下时，AI 评分会把还没轮到的那几个候选人（凑不满 3 人一组的零头）也补着评完，暂停期间两个进度条能对齐，不再停在「差一点没评完」的位置
- **打招呼速度稍加快**：自动打招呼之间的间隔从 3～5 秒微调到 2～5 秒，整体快一点点，仍保留随机间隔防止被限制

### v1.8.2 更新内容

- **修复：导出 Excel 里个别候选人教育经历识别错**——像「××大学××学院」这类独立学院（校名里连着「大学」「学院」两段），以前会被错切成一所学校加一个学院，专业里也混进「+」等奇怪符号；现在学校、专业、学历能正确分开（AI 用「+」分隔的格式也照常识别）。**已评完的批次不用重新提取**：重新点「导出 Excel」就能看到修正后的结果
- **发布上传更稳定**：改用更稳定的上传通道，发布过程不再有中途卡住的风险

## 更新内容（9月1日）

### v1.8.0 更新内容

- **历史记录里可以直接「评分」了**：打开历史记录，每个跑过提取的批次旁边都多了个「评分」按钮——不管上次是提了一半就取消，还是已经完整跑完，只要里面有简历就能直接评分，不用重新提取
- **跑完的批次也能重评**：以前已评完、导完 Excel 的批次只能看不能评；现在点「评分」会用已有简历重新评一遍（旧的评分会清掉重来，换模型或调了提示词后重评效果更明显）
- **「直接用上次数据评分」放宽**：以前只有「提取全部完成」才允许用上次数据评分，现在只要上次提取出来过简历就能用——哪怕只提了一部分就取消，也能接着这半批直接评分；数据被自动归档了也会自动找回来用
- **历史记录按钮精简**：历史里每个批次的操作按钮整合成「评分」，界面更清爽
- **打开历史记录更流畅**：读取历史记录的逻辑做了优化，点开抽屉更快、不卡

### v1.7.0 更新内容

- **提取中途可以「暂停」了**：批量提取候选人时，想歇一下或处理别的事，点「暂停」等当前这位读完就停下（最多再等几秒），按钮变成「▶ 继续」；点一下就从下一位接着提取，已提取的人不丢、评分照常进行，不用再为停一下整批取消
- **暂停 / 继续 / 跳过三个按钮图标统一**：暂停、继续、跳过提取候选人几个按钮的符号都换成同一套圆润图标，大小和位置完全一致，界面更整齐
- **「直接用上次数据评分」图标统一**：和「开始提取分析」用同一个播放图标，样式一致
- **完成页的 ✓ / ✕ 对齐修正**：成功对勾和失败叉号在圆圈里垂直居中了，不再偏上偏下
- **「打开输出目录」图标修正**：文件夹图标换成实心版，蓝色底上不再像下面空了一块

### v1.6.3 更新内容

- **岗位描述拖拽手柄再打磨**：右下角的拖拽斜线改成经典的「一长一短」样式，两条线间距拉开、整体更小巧；手柄现在真正贴在文本域右下角，离底边和侧边一样远（以前会偏下、紧贴着底边）；滚动条拖到底时和手柄之间留出一点空隙，不再挨在一起

### v1.6.2 更新内容

- **「未找到推荐页」弹窗改回工具样式**：检测到 Chrome 已打开但没开推荐牛人页时，弹出的确认框之前变成了系统样式，现在改回工具自己的弹窗，和其他弹窗长得一样
- **全部图标统一成一套**：设置齿轮、提示圆圈 i、关闭 ×、成功对勾、失败 ✕、眼睛，全部换成同一套圆润图标，界面更整齐
- **眼睛图标两状态一致**：密码框的显示/隐藏按钮，睁眼和闭眼两个图标现在大小、位置完全一样，点击切换时不会跳
- **「API 配置」齿轮对齐**：齿轮图标和标题文字在同一水平线上
- **提示 i 更简洁**：「电脑闲置」那个提示图标，去掉了外面的圆圈底色，颜色也调淡了
- **打开页面提示框换样式**：提醒去 Chrome 打开推荐牛人页的那条黄提示，去掉了左边深黄竖条，改成浅黄卡片，更清爽
- **眼睛悬浮小提示删除**：鼠标放到眼睛按钮上，不再弹「点击显示 / 点击隐藏」

### v1.6.1 更新内容

- **编辑岗位点「取消」回到目标岗位列表**：以前在「添加/编辑岗位」弹窗点取消，弹窗会直接全部关掉；现在会先返回「目标岗位」列表，可以接着选别的岗位或再加，不用重新打开
- **搜索框清空按钮样式改好**：「目标岗位」搜索框的 ✕ 清空按钮改成常见的「实心圆 + 空心×」样式，大小和颜色跟整体界面更协调
- **岗位描述拖拽手柄重做**：文本域右下角的拖拽手柄做得更小巧精致；拖拽时鼠标不会跑丢，松开也不会误碰弹窗边缘把它关掉；鼠标指针是上下箭头，一眼看出只能调高度、不能调宽度
- **编辑弹窗的岗位名称改成灰色**：已添加的岗位，名称在编辑弹窗里不能改，现在用灰色底 + 灰色字明显区分，一看就知道「只能改岗位描述，改不了名称」
- **API 配置的「眼睛」按钮对齐修好**：API Key、邮箱密码输入框右侧的显示/隐藏密码按钮，之前略偏下，现在和输入框垂直居中对齐

## 更新内容（8月31日）

### v1.6.0 更新内容

- **提取和评分并行，整体更快**：以前要等候选人全部提取完，才开始 AI 评分；现在提取到 15 人后，评分就和提取同时进行，边提取边打分，两个进度条并排往前走，不用干等
- **顶部显示当前在跑什么**：两个步骤同时在跑时，顶部会显示「并行：步骤1 + 步骤2」；提取阶段还会区分「正在扫描候选人列表…」和「正在逐个读取简历」两种状态，随时知道这一步在干嘛
- **提取结束的过渡更直观**：提取完成进入评分时，会先显示「共 X 人，X 个岗位」的总览，一眼看清这一批的整体情况
- **界面文字标点统一**：提示语里夹在中文之间的英文标点都换成了中文标点，观感更整齐

### v1.5.11 更新内容

- **候选人卡片箭头修正**：跑完一轮后点开候选人卡片，右边的提示箭头点开后现在是正正的「▼」，以前会歪向一边
- **目标岗位搜索框加「一键清空」**：搜索框里输入文字后，右边会出现一个 ✕ 小按钮，点一下就清空搜索、恢复显示全部岗位

## 更新内容（8月27日）

### v1.5.10 更新内容

- **评分维度不再和硬性条件重复**：以前 AI 提取「核心评估维度」时，可能会把学历、证书这类内容也当成评估维度提进去，和后面的「任职资格关键筛选项」撞车，同一个项目被扣两次分。现在明确禁止把学历、证书等硬性筛选项内容提进评估维度——维度只负责看软实力（能力、经验、行业背景等），学历、证书这类硬性条件统一走筛选项那一关
- **岗位描述提示词同步更新**：README 第 5 步「生成岗位描述」用的提示词也按同样的规则更新了，新生成的岗位描述和评分规则口径一致，不会再出现维度里混着学历证书的情况

## 更新内容（8月26日）

### v1.5.9 更新内容

- **「重启 Chrome」弹窗的按钮改成「取消」**：提示「重启 Chrome 进入『边用边跑』模式？」时，两个按钮分别是「重启 Chrome」和「取消」，和提示文字里说的「选『取消』」对得上（以前第二个按钮写的是「按普通方式运行」，和提示对不上）
- **使用说明的配图更新了**：README 里的示例截图已更新为最新界面

### v1.5.8 更新内容

- **滚动条变细变淡了**：以前列表滚动条又粗又显眼，现在改得很细、平时淡淡的几乎看不出来，鼠标放上去才变清楚，界面整体更干净
- **「继续提取」一进来就显示进度**：以前点「继续提取」后界面先显示「等待开始」，要等一会儿才动；现在立刻显示上次提取到哪了——如果只提取到一半，显示「已有 X 名候选人，正在继续提取剩余…」；如果提取已完成，直接显示步骤 1 已完成、从 AI 评分接着跑
- **邮件发送失败的提示更清楚**：以前完成页显示「查看邮件失败的处理办法」，比较笼统；现在折叠条标题直接写「邮件发送失败的原因」，点开能看到服务器返回的具体报错内容

### v1.5.7 更新内容

- **修好「历史记录」里两条信息和「时间未知」**：以前如果有一轮只开个头就中断了，它留下的空文件夹也会被当成一批历史记录列出来，导致历史记录里多出一条，而且当前这一批会显示「时间未知」。现在空批次不再显示，当前批次会显示真实的运行时间，也不会再误标成「未完成」
- **跑完的批次人数显示修好了**：以前跑完一批后，历史记录里这一批的人数会显示成 0；现在会正确显示这一批提取了多少人
- **完成页的「通过」标签更简洁**：去掉「通过（可打招呼）」里的「（可打招呼）」，只保留「通过」
- **修好「用该份数据重新评分」报错**：当前正在跑的这一批（有提取好的简历数据）也能点「用该份数据重新评分」了，不再提示「目标不是历史归档目录，无法重新评分」
- **修好黄色提示条透明看不清**：一些提示（比如报错提示）用的是黄色样式，以前这个样式没配背景色，弹出来是透明底、几乎看不清字。现在和其他提示条一样有背景色和边框

### v1.5.6 更新内容

- **「编辑」「添加」窗口过渡更顺滑**：点「编辑」或「+ 添加新岗位」时，原来的岗位列表窗口会先轻轻淡出、编辑/添加窗口再浮起，两个窗口像翻页一样自然交接，不再像以前那样「啪」地一下突然切换
- **弹窗开关更柔和**：目标岗位、编辑/添加、删除确认这些弹窗，打开和关闭都带上了平滑的淡入淡出过渡，整体更柔和；编辑弹窗打开时岗位描述也直接是填好的，不再出现「先空框、内容再突然塞进来」的突兀感

### v1.5.5 更新内容

- **目标岗位弹窗再修一层**：岗位名称和「编辑」「删除」按钮之间留出了间距，名字再长也不会贴到按钮上
- **点「编辑」更快了**：去掉了原来多余的等待，点了立刻弹出编辑窗口，岗位描述在后台读完会自动填进去，不再卡一下
- **字体彻底统一成一种**：整个软件改用 Windows 自带的「微软雅黑」，中文、英文、数字都是同一个字体，不再混着两种字体
- **使用许可换成 CC BY-NC-SA 4.0**：成熟常用的开源协议，条款为「署名 + 禁止商用 + 修改须同协议开源」，README 和仓库都已更新

### v1.5.4 更新内容

- **API 地址的填写示例改清楚了**：输入框和说明文档里的示例统一成 `https://api.example.com`，不再出现让人困惑的 `XXX.XXX.XXX.XX` 占位符
- **字体统一了**：之前「API 配置」输入框里的提示语，英文是一种字体、中文是另一种字体，看着不整齐；现在全软件所有输入框、提示语都统一成同一套字体
- **目标岗位弹窗排版修好了**：比较长的岗位名称现在会自动换行，不再挤到「编辑」按钮上；已选中的岗位名太长时也会自动省略号收尾
- **新增「使用许可」条款**：软件仅供个人学习使用、禁止商用；允许修改但修改后必须保持开源；转载须保留署名。README 和仓库里都写明了

### v1.5.3 更新内容

- **邮箱要填完整地址了**：邮件通知的邮箱必须填完整格式（含 @，如 `hr@example.com`），不再自动补任何公司域名。软件以后可以放心分享给其他公司的人使用
- **移除默认发件邮箱**：以前程序里内置了一个默认发件账号，现在已去掉，发件邮箱由你自己填（填一次会保存，之后不用重复填）

### v1.5.2 更新内容

- **按钮再缩小一档**：「编辑」「删除」「保存配置」以及弹窗里的按钮都又收紧了一圈，整体更紧凑
- **「暂无历史记录」提示居中**：打开历史记录抽屉时，没有数据的那行提示文字移到正中间，不再偏在下方

## 更新内容（8月25日）

### v1.5.1 更新内容

- **按钮整体变小、一屏放下**：主按钮、卡片、切换按钮都收紧了一圈，折叠「API 配置」时所有内容正好一屏显示，不再出现右侧滚动条
- **「开始提取分析」和「直接用上次数据评分」挨得更紧了**：两个按钮之间的大空隙去掉了
- **去掉「请选择岗位」右边的小箭头 ▶**，只留文字，界面更干净
- **修好了编辑岗位被挡住**：点岗位「编辑」后，现在会先关掉「目标岗位」弹窗再打开编辑弹窗，不再被挡
- **保存配置的提示改简单了**：点「保存配置」成功后只弹「配置已保存」，不再啰嗦

### v1.5.0 更新内容

**界面焕新**

- **整体视觉升级**：延续蓝色主色调，升级了字体、按钮/进度条/提示条组件、动效和留白，界面更清爽现代
- **键盘也能顺畅操作**：所有弹窗都能用键盘走完（Tab / 方向键 / 回车 / Esc），补上了焦点环和读屏支持，部分文字可以选中复制
- **确认弹窗统一**：「重启 Chrome」「跳过提取」「删除岗位」等系统确认框，统一换成软件自带的美观弹窗，写明风险后再确认
- **文案更清楚**：岗位描述输入框会提示「勿粘贴原始 JD」；邮箱密码的说明挪到了输入框提示里；打招呼相关说法统一为「打招呼」

**打分结果更好看**

- **完成页结果一目了然**：跑完后直接显示这一批的统计（提取 N 人 / 通过 M 人 / 平均分 / 通过率）、五星到一星的档位分布条，还能展开看每个候选人的分数、档位和 AI 评语，不用再打开 Excel 核对
- **打招呼进度条真实推进**：以前一直停在 0%，现在会显示「正在打招呼 3/80」这样的实时进度

**历史记录 + 继续提取**

- **新增「历史记录」**：「API 配置」卡片里「输出目录」那行多了「历史记录」按钮，点开列出最近跑过的每一批（时间、来源、人数、状态）。每一批都可以打开输出目录、删除、**继续提取**（提取中途取消后，接着上次的位置继续跑，不重复）、**用这份数据重新评分**（换好模型/岗位描述后直接重评，不重新提取）
- **取消后能接着跑**：提取中途取消时，会提示「已保存进度，可在历史记录里继续提取」

### v1.4.8 更新内容

- **新增「直接用上次数据评分」按钮**：换好模型/岗位描述后，不用重新提取，直接用上次提取的候选人数据重新评分（即使上次的数据目录已被自动归档，软件也能自动找回；没有可用数据时按钮是置灰的）
- **推荐模型换成 `GLM-5_SLB`**：文档里建议用的模型从 `qwen3.5-35b-a3b_Local` 换成了 `GLM-5_SLB`（其他能用的模型照样可以填）
- **保存配置有提示了**：点「保存配置」成功后，屏幕中心偏上会弹出「配置已保存」的绿色提示，确认没点错
- **折叠配置时一屏放下**：收紧了一些卡片间距和页面留白，折叠「API 配置」时所有内容正好一屏显示，不再出现右侧滚动条

## 更新内容（8月24日）

### v1.4.7 更新内容

- **API 配置更好用了**：API 地址旁标注了「Anthropic 格式」；API Key 和邮箱密码框加了**眼睛按钮**——点击可以临时显示明文（方便核对填的对不对），再点一下变回圆点隐藏
- **Chrome 没开也能直接开始**：以前电脑上没开 Chrome 就点「开始提取分析」会直接报连接失败；现在会自动用「边用边跑」模式启动 Chrome，并**直接打开你选的提取来源页面**（推荐牛人页 / 搜索页 / 沟通页），等页面加载好再点一次开始即可

## 更新内容（8月21日）

### v1.4.6 更新内容

- **去掉输入框的一键清空 × 按钮**：API 配置的输入框（API 地址、Key、模型名称、邮箱、邮箱密码）以前鼠标悬停时右侧会出现一个清空内容的 ×，现在去掉了，界面更清爽（浏览器自带的清空 × 也一并隐藏了）

### v1.4.5 更新内容

- **评分更稳定了**：之前一部分候选人会提示「评分失败：解析失败」，主要原因是 AI 接口返回的内容里会带一长段模型的「思考过程」，软件以前把这段思考当成答案来读，就读不出评分了。现在软件会自动跳过思考部分、只读真正的评分结果，并允许 AI 输出更长的内容，评语不容易被截断
- **评分失败会自动重试，不再整批丢**：如果一批 3 人整体评分失败，软件会自动拆成 1 人 1 人逐个重新评分。个别候选人实在评不了才会标失败，不会像以前那样一批人集体丢分
- **提取更快了**：扫描候选人列表、复制简历、滚动到底、关闭弹窗等环节的等待时间都进一步缩短，同样 300 人提取大约能快 10～15 分钟
- **模型推荐更新**：推荐使用 `qwen3.5-35b-a3b_Local`（AI 服务里能用的其他模型也可以填）
- **评语格式微调**：学历核查部分的示例更简洁

## 更新内容（8月20日）

### v1.4.4 更新内容

- **新增「开启模拟复制提取」开关**：默认不勾选。电脑闲置时勾上，提取更快（画布型简历走「模拟复制」读出全文，比截图识别快很多）；在用电脑时保持不勾选，软件就完全不碰系统剪贴板，不会干扰你复制粘贴
- **日志更清楚了**：之前跑「模拟复制」时日志却写「DOM提取」，容易让人以为复制没生效。现在日志如实区分「DOM提取」和「模拟复制提取」，走到哪一步一眼能看出来
- **三个页面的提示语改短了**：推荐牛人页、搜索页、沟通页的提示更简洁，沟通页会提示先选「未读」
- **使用提醒更新**：提取时**不要点 Chrome 的「最小化」按钮**（最小化会导致截图变形、内容不全），想边跑边用电脑时，用别的窗口把 Chrome 盖住即可

## 更新内容（8月18日）

### v1.4.0 更新内容

- **简历是图片（canvas）的候选人也终于能直接提取了**：BOSS直聘有一部分候选人的简历是"画在画布上的图片"，文字没法直接读，以前这类简历只能靠截图识别（慢、还容易认错字）。现在软件会自动模拟你手动「选中 → 往下滚 → 复制」的操作，把整份简历全文复制出来，又快又准；只有复制失败时才退回截图识别
- **邮件密码提示改准确了**：以前密码填不对只提示"找 IT 确认密码"，现在会分情况告诉你最可能是哪种原因——最常见的是公司邮箱开启了「三方客户端安全密码」，要填邮箱设置里获取的**客户端安全密码**（不是登录密码）；网页版邮箱能发但软件报错、网页版也登不上、账号被锁定等情况都有对应的处理办法
- **导出的教育经历更可靠**：以前从简历文本里认学校名容易把证书、页面广告误认成学校，现在导出前会用 AI 评语里的「第一学历 / 最高学历」校一遍，把假的学校条目去掉
- **评分更细**：AI 评语里的教育经历现在会带上就读时间（比如 2016-2020），学校名看不清时也允许从上下文推断并标注
- **截图更快了**：各环节的等待时间缩短，截图识别整体提速
- **提取失败时日志更好查**：如果某个候选人提取不到，日志会写清楚卡在哪一步（是简历本身是图片没法读，还是页面没加载好），方便排查是软件问题还是这个候选人确实读不了
- **兼容新版 Chrome**：Chrome 新版改动了调试接口，软件已适配，换新 Chrome 也能正常提取

### v1.4.1 更新内容

- **修复：沟通页模式下，画布简历「模拟选中复制」复制不全**：以前在「沟通页」提取时，画布简历的模拟复制不会往下滚动，只复制到第一屏就停（推荐牛人页/搜索页正常）。现在软件会自动识别每个页面的滚动区域，沟通页也能整份滚到底、复制全文
- **防旧代码残留**：如果电脑里残留着旧版本的「后台帮手」，软件会检测到并自动换新，避免还在用修好之前的旧代码

### v1.4.2 更新内容

- **读取岗位描述（JD）更快了**：以前点开岗位弹窗读 JD 时有两处固定等待——弹窗其实早弹出来了，也要硬等 0.5～0.8 秒；关弹窗时还要再无条件空等 1 秒多。现在改成「弹窗好了就读、关了就继续」，每个新岗位的停顿从 2～3 秒降到 1 秒左右
- **关闭弹窗（看完简历后）也顺带变快**：关闭逻辑里那处"弹窗明明关掉了还干等 1 秒多"的空等去掉了，每个候选人省一点时间
